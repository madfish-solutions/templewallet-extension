export enum AccountsTestData {
  defaultSeedPhrase = 'default seed',
  defaultPassword = 'default password',
  secondSeedPhrasePrivateKey = 'private key of second seed',
  CreatedOrRestoredAccountPrivateKey = 'private key of created account'
}
