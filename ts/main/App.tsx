import * as React from "react";
import WidthContainer from "main/layouts/WidthContainer";

const App: React.FC = () => (
  <WidthContainer>
    <div>KEK LAL plS</div>
  </WidthContainer>
);

export default App;
