import React, { memo } from 'react';

interface SelectAccountStepProps {
  onSuccess: EmptyFn;
}

export const SelectAccountStep = memo<SelectAccountStepProps>(({ onSuccess }) => {
  return <div />;
});
